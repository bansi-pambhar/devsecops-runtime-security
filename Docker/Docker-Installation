#Why Docker?
•	Consistent builds
•	Container scanning
•	Kubernetes deployment
#Containers ensure build reproducibility and security scanning.

**commands**

#insatll docker
curl -fsSL https://get.docker.com | sudo sh
#Add user to docker group
sudo usermod -aG docker $USER
newgrp docker
#Verify
docker run hello-world
