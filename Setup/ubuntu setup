#update the system
sudo apt update && sudo apt upgrade -y
#install basic tools
sudo apt install -y git curl wget unzip apt-transport-https ca-certificates software-properties-common
#verify
git --version
curl --version
